// 検索結果ページを制作するブランドごとに適宜、ROOT pathを変更すること
const ROOT = './htdocs/www-kmshair-com';

const sass = {
  src: [
    `${ROOT}/_scss/**/*.scss`
  ],
  dest: `${ROOT}/css/`,
  output: 'style.css',  // 出力ファイル名
  autoprefixer: {
    browsers: ["iOS >= 9.0", "android >= 4.2", "ie >= 11"]
  }
};

module.exports.sass = sass;
